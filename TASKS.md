# TASKS.md

## 任务总览

本文件基于当前项目真实代码和文档审查整理。未确认内容均标记为“待确认”，不作为已实现能力描述。

## 高优先级

### 1. 对齐 mock 配置与真实行为

状态：待处理

现状：

- `USE_MOCK_REMOTE`、`USE_MOCK_CRYPTO` 在配置和 README 中存在。
- 查询执行链路当前仍要求 SM2 公钥，并走真实加密与真实 httpx 调用。

建议：

- 方案 A：真正实现 mock 远端和 mock 加密分支。
- 方案 B：删除或弱化 mock 文档描述，明确当前只支持真实链路。

### 2. 开放 API 密钥安全加固

状态：待处理

现状：

- `dq_open_api_client.app_secret_hash` 字段名为 hash。
- `authenticate_openapi_client` 当前直接用明文比较。
- 种子脚本直接写入 `demo_app_secret`。

建议：

- 使用密码哈希或 HMAC 存储 AppSecret。
- 仅在创建/重置时展示明文一次。
- 更新 `docs/open-api-doc.md` 和种子脚本说明。

### 3. 审计日志实现或修正文档

状态：待处理

现状：

- `dq_audit_log` 表和模型存在。
- README 写明关键操作写入审计表。
- 当前未发现实际写入 `DqAuditLog` 的代码。

建议：

- 若需要审计：为登录、接口配置、参数模板、导出等写入审计记录。
- 若暂不实现：修正 README，把审计标记为预留能力。

### 4. 核心查询链路测试

状态：待处理

现状：

- 未发现测试文件。
- 查询执行链路涉及参数、加密、远端调用、解密、落库、解析，风险较高。

建议：

- 增加后端单元测试或集成测试。
- 优先覆盖参数校验、结果解析、payload 存储、开放 API 鉴权、导出。

### 5. 敏感数据与样本 payload 清理策略

状态：待确认

现状：

- 仓库包含 `backend/storage/query_payloads/` 下的历史响应样本。
- `docs/interface_dev_doc_weihai.md` 中包含 token 和 SM2 公钥示例。

建议：

- 确认这些数据是否允许入库。
- 若不允许，迁移到安全配置或脱敏样本。
- 增加 `.gitignore` 规则，避免继续提交运行期 payload。

## 中优先级

### 6. 默认值一致性整理

状态：待处理

现状：

- 接口配置默认值在迁移、模型、服务和前端间不完全一致。
- 涉及 `encrypt_key_header_name`、`response_msg_field_name`、`success_code_value` 等字段。

建议：

- 确认真实接口默认约定。
- 统一 Alembic、ORM、创建服务、前端表单默认值。

### 7. Redis、缓存和限流能力落地

状态：待确认

现状：

- Redis 依赖、配置、Compose 服务存在。
- `cache_ttl_seconds`、`rate_limit_qps` 字段存在。
- 当前未发现实际缓存和限流逻辑。

建议：

- 确认是否近期需要。
- 如不需要，在文档中标注为预留。
- 如需要，优先实现开放 API 限流和接口配置缓存。

### 8. RBAC 权限控制

状态：待确认

现状：

- 用户、角色、用户角色表存在。
- 当前管理 API 主要只要求登录用户。

建议：

- 确认是否需要管理员、审计员、普通查询员等角色。
- 若需要，增加权限模型和前后端控制。

### 9. 开放 API 客户端管理页面

状态：待处理

现状：

- 后端表和鉴权逻辑存在。
- 前端没有客户端创建、启停、重置密钥页面。

建议：

- 增加客户端管理 UI。
- 支持启停、重置 Secret、查看调用日志。

### 10. IP 白名单校验

状态：待处理

现状：

- `ip_whitelist` 字段存在。
- 开放 API 鉴权未使用该字段。

建议：

- 根据 `X-Forwarded-For` / `X-Real-IP` / 客户端 IP 实现校验。
- 明确反代场景下可信代理策略。

## 低优先级

### 11. 动态表单增强

状态：待处理

现状：

- 支持 input/select/date 基础组件。
- 必填校验、select options、日期格式等能力较弱。

建议：

- 使用参数模板中的规则驱动前端校验。
- 明确 `ext_json.options` 的维护方式。

### 12. 导出任务异步化

状态：待确认

现状：

- 导出接口同步生成 CSV/XLSX。
- `dq_export_task` 表具备任务状态字段。

建议：

- 数据量较大时改为后台任务。
- Redis/Celery/APScheduler 方案待确认。

### 13. Dashboard 完善

状态：待处理

现状：

- `Dashboard.vue` 目前只是简单欢迎文案。

建议：

- 展示接口数量、今日查询量、失败率、最近失败记录等。
- 数据来源和统计口径待确认。

### 14. 文档文件名乱码处理

状态：待确认

现状：

- `docs/` 下部分中文文件名显示乱码。

建议：

- 确认原始文件名和编码。
- 统一重命名并更新引用。

### 15. 前端刷新后的用户态恢复

状态：待处理

现状：

- localStorage 有 token 时路由认为已登录。
- 刷新后 `auth.user` 可能为空，顶部用户信息不显示。

建议：

- 应用启动或路由守卫中补充 `fetchMe`。

## 待确认问题

- 生产数据库是否确认为瀚高 MySQL 兼容模式。
- `/docs` 和 `/openapi.json` 在生产是否允许暴露。
- 威海相关 token、公钥和接口地址是否只是开发样例。
- 是否需要完整审计闭环。
- 是否需要将开放 API 作为主要对外集成方式。
- 是否需要保留仓库中的历史 payload 样本。
